import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: "column",
    padding: 70,
    alignItems: "center",
  },
  text: {
    marginTop: 30,
    textAlign: "center",
  },
});
