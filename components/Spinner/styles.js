import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: "column",
    padding: 60,
    alignItems: "center",
  },
  text: {
    marginTop: 30,
    textAlign: "center",
  },
});
